This project uses uv to manage its dependencies and virtual environments. Anytime a python command is to be run use the following syntax:
```
uv run <file> <args>
```
Instead of:
```
python <file> <args>
```